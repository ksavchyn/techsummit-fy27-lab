import { LakebasePoolConfig, Logger } from "./types.js";
import { DriverTelemetry } from "./telemetry.js";

//#region src/token-refresh.d.ts
interface TokenRefreshDeps {
  userConfig: Partial<LakebasePoolConfig>;
  endpoint: string;
  telemetry: DriverTelemetry;
  logger?: Logger;
}
/**
 * Build the password callback with token caching, deduplication, and telemetry.
 *
 * The returned async function is called by `pg.Pool` each time a new connection
 * is established. It caches OAuth tokens and deduplicates concurrent refresh
 * requests so only one API call is made even under parallel connection creation.
 */
declare function createTokenRefreshCallback(deps: TokenRefreshDeps): () => Promise<string>;
//#endregion
export { TokenRefreshDeps, createTokenRefreshCallback };
//# sourceMappingURL=token-refresh.d.ts.map