import { REGISTRY_INDEX_API_URL, REGISTRY_INDEX_URL, REGISTRY_REPO, resolveToken } from "./constants.js";
import { registryAuthHeaders } from "./client.js";
import { Command } from "commander";
import process from "node:process";
import pc from "picocolors";

//#region src/cli/commands/registry/list.ts
function isVerified(item) {
	return item.meta?.verified === true;
}
/** Free-text haystack for search matching. */
function searchHaystack(item) {
	return [
		item.name,
		item.title ?? "",
		item.description ?? "",
		itemKind(item),
		...item.categories ?? []
	].join(" ").toLowerCase();
}
/** True if every whitespace-separated term in `query` appears in the item. */
function matchesQuery(item, query) {
	const haystack = searchHaystack(item);
	return query.toLowerCase().split(/\s+/).filter(Boolean).every((term) => haystack.includes(term));
}
/** A friendly kind for the TYPE column: plugin, component, hook, theme, … */
function itemKind(item) {
	if ((item.files ?? []).some((f) => f.target?.endsWith("manifest.json") || f.path?.endsWith("manifest.json"))) return "plugin";
	switch (item.type) {
		case "registry:component":
		case "registry:block": return "component";
		case "registry:hook": return "hook";
		case "registry:lib": return "lib";
		case "registry:theme": return "theme";
		case "registry:ui": return "ui";
		case "registry:page": return "page";
		default: return item.type?.replace(/^registry:/, "") || "item";
	}
}
const KIND_COLOR = {
	plugin: pc.magenta,
	component: pc.blue,
	hook: pc.cyan,
	theme: pc.yellow,
	lib: pc.green
};
function printTable(items) {
	if (items.length === 0) {
		console.log(pc.dim("No items found in the registry."));
		return;
	}
	const kinds = items.map(itemKind);
	const maxName = Math.max(4, ...items.map((i) => i.name.length));
	const maxKind = Math.max(4, ...kinds.map((k) => k.length));
	const header = `${"NAME".padEnd(maxName)}  ${"TYPE".padEnd(maxKind)}  VERIFIED  DESCRIPTION`;
	console.log(pc.bold(header));
	console.log(pc.dim("─".repeat(header.length)));
	for (const [i, item] of items.entries()) {
		const verified = isVerified(item);
		const kind = kinds[i];
		const colorKind = KIND_COLOR[kind] ?? pc.white;
		const name = pc.cyan(item.name.padEnd(maxName));
		const kindCell = colorKind(kind.padEnd(maxKind));
		const mark = verified ? pc.green("✓".padEnd(8)) : " ".repeat(8);
		const desc = item.description ?? item.title ?? "";
		console.log(`${name}  ${kindCell}  ${mark}  ${verified ? desc : pc.dim(desc)}`);
	}
}
/** Fetches the registry index (token-aware), or exits with a helpful message. */
async function fetchIndex() {
	const token = resolveToken();
	const url = token ? REGISTRY_INDEX_API_URL : REGISTRY_INDEX_URL;
	let res;
	try {
		res = await fetch(url, { headers: registryAuthHeaders(token) });
	} catch (err) {
		console.error(pc.red(`Failed to reach the registry at ${url}`));
		console.error(`  ${err instanceof Error ? err.message : String(err)}`);
		process.exit(1);
	}
	if (res.status === 404 || res.status === 401 || res.status === 403) {
		console.error(pc.red(`Could not read the registry index from ${REGISTRY_REPO} (HTTP ${res.status}).`));
		if (!token) console.error("  If the repo is private, set APPKIT_REGISTRY_TOKEN (or GITHUB_TOKEN) to a token with read access.");
		process.exit(1);
	}
	if (!res.ok) {
		console.error(pc.red(`Registry returned HTTP ${res.status} for ${url}`));
		process.exit(1);
	}
	return (await res.json()).items ?? [];
}
function output(items, opts) {
	if (opts.json) console.log(JSON.stringify(items.map((i) => ({
		...i,
		kind: itemKind(i),
		verified: isVerified(i)
	})), null, 2));
	else printTable(items);
}
async function runList(opts) {
	let items = await fetchIndex();
	if (opts.verified) items = items.filter(isVerified);
	output(items, opts);
}
async function runSearch(query, opts) {
	let items = await fetchIndex();
	items = items.filter((i) => matchesQuery(i, query));
	if (opts.verified) items = items.filter(isVerified);
	if (items.length === 0 && !opts.json) {
		console.log(pc.dim(`No items match "${query}".`));
		return;
	}
	output(items, opts);
}
const registryListCommand = new Command("list").description("List items available in the AppKit registry").option("--json", "Output as JSON").option("--verified", "Show only verified items").action((opts) => runList(opts).catch((err) => {
	console.error(err);
	process.exit(1);
}));
const registrySearchCommand = new Command("search").description("Search registry items by name, description, type, or keyword").argument("<query...>", "Search terms (all must match)").option("--json", "Output as JSON").option("--verified", "Show only verified items").addHelpText("after", `
Examples:
  $ appkit registry search chart
  $ appkit registry search kpi dashboard
  $ appkit registry search plugin --json`).action((query, opts) => runSearch(query.join(" "), opts).catch((err) => {
	console.error(err);
	process.exit(1);
}));

//#endregion
export { registryListCommand, registrySearchCommand };
//# sourceMappingURL=list.js.map