import { printReport, printReportJson } from "./report.js";
import { runDoctor } from "./run.js";
import fs from "node:fs";
import { Command } from "commander";
import process from "node:process";
import dotenv from "dotenv";

//#region src/cli/commands/doctor/index.ts
/**
* Loads an explicit env file into `process.env`, overriding the auto-loaded
* `.env`. Throws if the file is missing.
*/
function loadEnvFile(envFile) {
	if (!fs.existsSync(envFile)) throw new Error(`env file not found: ${envFile}`);
	dotenv.config({
		path: envFile,
		override: true
	});
}
async function runDoctorCommand(options) {
	if (options.envFile) loadEnvFile(options.envFile);
	const report = await runDoctor(options);
	if (options.json) printReportJson(report, options.detail);
	else printReport(report, options.detail);
	process.exit(report.exitCode);
}
const doctorCommand = new Command("doctor").description("Diagnose an AppKit app's Databricks resources: authentication, config, and resource existence/reachability").option("-p, --profile <name>", "Databricks CLI profile to authenticate with").option("-e, --env-file <path>", "Load this env file before checking (overrides the auto-loaded .env), e.g. .env.local").option("-d, --detail", "Show full underlying error messages (raw SDK output) for failures").option("--json", "Output the diagnostic report as JSON").addHelpText("after", `
Examples:
  $ appkit doctor
  $ appkit doctor --profile my-profile
  $ appkit doctor --env-file .env.local
  $ appkit doctor --detail
  $ appkit doctor --json`).action((opts) => runDoctorCommand(opts).catch((err) => {
	console.error(err);
	process.exit(1);
}));

//#endregion
export { doctorCommand };
//# sourceMappingURL=index.js.map