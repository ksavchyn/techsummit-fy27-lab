import { Binary, Bool, DateDay, Decimal, DurationMicrosecond, Field, Float32, Float64, Int16, Int32, Int64, Int8, IntervalYearMonth, List, Map_, Null, Schema, Struct, Table, TimestampMicrosecond, Utf8, tableToIPC } from "apache-arrow";

//#region src/connectors/sql-warehouse/arrow-schema.ts
/**
* Parse a Databricks SQL type text (the value returned by the Statement
* Execution API in `ColumnInfo.type_text`) into an Apache Arrow DataType.
*
* Supports:
*   - All scalar types (STRING, INT, BIGINT, DECIMAL, TIMESTAMP, etc.)
*   - Parameterized scalars: DECIMAL(p,s), VARCHAR(n), CHAR(n)
*   - Nested types: ARRAY<T>, MAP<K,V>, STRUCT<f1:T1, f2:T2 COMMENT '...', ...>
*   - INTERVAL year-month and day-time variants
*   - Backtick-quoted struct field names with embedded `` `` `` escapes
*
* Unknown or unparseable types fall back to Utf8 — empty-Table consumers
* still see a column with the right name; only the inner type is degraded.
*/
function parseDatabricksType(typeText) {
	const parser = new TypeParser(typeText);
	const result = parser.parseType();
	parser.expectEnd();
	return result;
}
/**
* Build an empty Arrow IPC stream (base64-encoded) matching the column schema
* returned by the warehouse. Used so ARROW_STREAM responses with no rows still
* deliver a real Arrow Table to the client, preserving the hook's typed
* contract.
*/
function buildEmptyArrowIPCBase64(columns) {
	const ipc = tableToIPC(new Table(new Schema(columns.map((col, index) => {
		const typeText = col.type_text ?? col.type_name ?? "STRING";
		let dataType;
		try {
			dataType = parseDatabricksType(typeText);
		} catch {
			dataType = new Utf8();
		}
		return new Field(col.name && col.name.length > 0 ? col.name : `column_${index}`, dataType, true);
	}))), "stream");
	return Buffer.from(ipc).toString("base64");
}
var TypeParser = class {
	input;
	pos = 0;
	constructor(input) {
		this.input = input;
	}
	parseType() {
		this.skipWs();
		let name;
		if (this.peek() === "`") name = this.consumeBacktickIdent();
		else name = this.consumeIdent();
		const upper = name.toUpperCase();
		this.skipWs();
		if (upper === "INTERVAL") return this.parseInterval();
		if (this.peek() === "(") {
			this.consume("(");
			const args = this.parseNumberArgs();
			this.consume(")");
			this.skipWs();
			return this.makeParameterized(upper, args);
		}
		if (this.peek() === "<") {
			this.consume("<");
			const result = this.makeGeneric(upper);
			this.skipWs();
			this.consume(">");
			return result;
		}
		return this.makeScalar(upper);
	}
	expectEnd() {
		this.skipWs();
		if (this.pos < this.input.length) throw new Error(`Unexpected trailing input at position ${this.pos}: "${this.input.slice(this.pos)}"`);
	}
	makeScalar(upper) {
		switch (upper) {
			case "STRING":
			case "VARIANT": return new Utf8();
			case "VARCHAR":
			case "CHAR": return new Utf8();
			case "BINARY":
			case "GEOGRAPHY":
			case "GEOMETRY": return new Binary();
			case "BOOLEAN":
			case "BOOL": return new Bool();
			case "TINYINT":
			case "BYTE": return new Int8();
			case "SMALLINT":
			case "SHORT": return new Int16();
			case "INT":
			case "INTEGER": return new Int32();
			case "BIGINT":
			case "LONG": return new Int64();
			case "FLOAT":
			case "REAL": return new Float32();
			case "DOUBLE": return new Float64();
			case "DECIMAL":
			case "NUMERIC":
			case "DEC": return new Decimal(0, 10, 128);
			case "DATE": return new DateDay();
			case "TIMESTAMP":
			case "TIMESTAMP_LTZ": return new TimestampMicrosecond("UTC");
			case "TIMESTAMP_NTZ": return new TimestampMicrosecond();
			case "VOID":
			case "NULL": return new Null();
			default: return new Utf8();
		}
	}
	makeParameterized(upper, args) {
		switch (upper) {
			case "DECIMAL":
			case "NUMERIC":
			case "DEC": {
				const precision = args[0] ?? 10;
				return new Decimal(args[1] ?? 0, precision, 128);
			}
			case "VARCHAR":
			case "CHAR": return new Utf8();
			default: return new Utf8();
		}
	}
	makeGeneric(upper) {
		switch (upper) {
			case "ARRAY": return new List(new Field("item", this.parseType(), true));
			case "MAP": {
				const keyType = this.parseType();
				this.skipWs();
				this.consume(",");
				this.skipWs();
				const valueType = this.parseType();
				return new Map_(new Field("entries", new Struct([new Field("key", keyType, false), new Field("value", valueType, true)]), false), false);
			}
			case "STRUCT": return this.parseStructFields();
			default:
				this.skipBalancedAngles();
				return new Utf8();
		}
	}
	parseStructFields() {
		const fields = [];
		while (true) {
			this.skipWs();
			if (this.peek() === ">") break;
			let name;
			if (this.peek() === "`") name = this.consumeBacktickIdent();
			else name = this.consumeIdent();
			this.skipWs();
			this.consume(":");
			this.skipWs();
			const type = this.parseType();
			this.skipWs();
			while (this.peekKeyword("NOT")) {
				this.consumeIdent();
				this.skipWs();
				if (this.peekKeyword("NULL")) this.consumeIdent();
				this.skipWs();
			}
			if (this.peekKeyword("COMMENT")) {
				this.consumeIdent();
				this.skipWs();
				this.consumeStringLiteral();
				this.skipWs();
			}
			fields.push(new Field(name, type, true));
			this.skipWs();
			if (this.peek() === ",") this.consume(",");
			else break;
		}
		return new Struct(fields);
	}
	parseInterval() {
		const seen = [];
		while (this.pos < this.input.length) {
			this.skipWs();
			const c = this.peek();
			if (c === "" || c === "," || c === ">" || c === ")") break;
			const word = this.consumeIdent().toUpperCase();
			seen.push(word);
		}
		return seen.some((w) => w === "YEAR" || w === "MONTH") ? new IntervalYearMonth() : new DurationMicrosecond();
	}
	parseNumberArgs() {
		const args = [];
		while (true) {
			this.skipWs();
			if (this.peek() === ")") break;
			args.push(this.consumeNumber());
			this.skipWs();
			if (this.peek() === ",") this.consume(",");
			else break;
		}
		return args;
	}
	peek() {
		return this.input[this.pos] ?? "";
	}
	peekKeyword(word) {
		if (this.input.slice(this.pos, this.pos + word.length).toUpperCase() !== word.toUpperCase()) return false;
		const next = this.input[this.pos + word.length] ?? "";
		return !/[A-Za-z0-9_]/.test(next);
	}
	consume(expected) {
		if (this.peek() !== expected) throw new Error(`Expected "${expected}" at position ${this.pos}, got "${this.peek()}" in "${this.input}"`);
		this.pos++;
	}
	skipWs() {
		while (this.pos < this.input.length && /\s/.test(this.input[this.pos] ?? "")) this.pos++;
	}
	consumeIdent() {
		const start = this.pos;
		while (this.pos < this.input.length && /[A-Za-z0-9_]/.test(this.input[this.pos] ?? "")) this.pos++;
		if (this.pos === start) throw new Error(`Expected identifier at position ${this.pos}, got "${this.peek()}" in "${this.input}"`);
		return this.input.slice(start, this.pos);
	}
	consumeBacktickIdent() {
		this.consume("`");
		let value = "";
		while (this.pos < this.input.length) {
			if (this.input[this.pos] === "`") {
				if (this.input[this.pos + 1] === "`") {
					value += "`";
					this.pos += 2;
					continue;
				}
				break;
			}
			value += this.input[this.pos];
			this.pos++;
		}
		this.consume("`");
		return value;
	}
	consumeNumber() {
		const start = this.pos;
		while (this.pos < this.input.length && /[0-9]/.test(this.input[this.pos] ?? "")) this.pos++;
		if (this.pos === start) throw new Error(`Expected number at position ${this.pos}, got "${this.peek()}" in "${this.input}"`);
		return Number.parseInt(this.input.slice(start, this.pos), 10);
	}
	consumeStringLiteral() {
		const quote = this.peek();
		if (quote !== "'" && quote !== "\"") throw new Error(`Expected string literal at position ${this.pos}, got "${quote}" in "${this.input}"`);
		this.pos++;
		let value = "";
		while (this.pos < this.input.length) {
			const c = this.input[this.pos];
			if (c === "\\") {
				const next = this.input[this.pos + 1];
				if (next !== void 0) {
					value += next;
					this.pos += 2;
					continue;
				}
				this.pos++;
				continue;
			}
			if (c === quote) {
				this.pos++;
				return value;
			}
			value += c;
			this.pos++;
		}
		throw new Error(`Unterminated string literal in "${this.input}"`);
	}
	skipBalancedAngles() {
		let depth = 1;
		while (this.pos < this.input.length && depth > 0) {
			const c = this.peek();
			if (c === "<") depth++;
			else if (c === ">") {
				depth--;
				if (depth === 0) return;
			}
			this.pos++;
		}
	}
};

//#endregion
export { buildEmptyArrowIPCBase64 };
//# sourceMappingURL=arrow-schema.js.map