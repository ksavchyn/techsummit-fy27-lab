import { createLogger } from "../../logging/logger.js";
import { TelemetryManager } from "../../telemetry/telemetry-manager.js";
import { SpanKind, SpanStatusCode } from "../../telemetry/index.js";
import { contextFromAbortSignal } from "../context.js";

//#region src/connectors/ai-search/client.ts
const logger = createLogger("connectors:ai-search");
var AiSearchConnector = class {
	telemetry;
	constructor(config = {}) {
		this.telemetry = TelemetryManager.getProvider("ai-search", config.telemetry);
	}
	async query(workspaceClient, params, signal) {
		if (signal?.aborted) throw new Error("Query cancelled before execution");
		const body = {
			columns: params.columns,
			num_results: params.numResults,
			query_type: params.queryType.toUpperCase(),
			debug_level: 1
		};
		if (params.queryText) body.query_text = params.queryText;
		if (params.queryVector) body.query_vector = params.queryVector;
		if (params.filters && Object.keys(params.filters).length > 0) body.filters_json = JSON.stringify(params.filters);
		if (params.reranker) body.reranker = {
			model: "databricks_reranker",
			parameters: { columns_to_rerank: params.reranker.columnsToRerank }
		};
		logger.debug("Querying VS index %s (type=%s, num_results=%d)", params.indexName, params.queryType, params.numResults);
		return this.telemetry.startActiveSpan("ai-search.query", {
			kind: SpanKind.CLIENT,
			attributes: {
				"db.system": "databricks",
				"vs.index_name": params.indexName,
				"vs.query_type": params.queryType,
				"vs.num_results": params.numResults,
				"vs.has_filters": !!(params.filters && Object.keys(params.filters).length > 0),
				"vs.has_reranker": !!params.reranker
			}
		}, async (span) => {
			const startTime = Date.now();
			try {
				const response = await workspaceClient.apiClient.request({
					method: "POST",
					path: `/api/2.0/vector-search/indexes/${params.indexName}/query`,
					payload: body,
					headers: new Headers({ "Content-Type": "application/json" }),
					raw: false,
					query: {}
				}, contextFromAbortSignal(signal));
				const duration = Date.now() - startTime;
				span.setAttribute("vs.result_count", response.result.row_count);
				span.setAttribute("vs.query_time_ms", response.debug_info?.response_time ?? 0);
				span.setAttribute("vs.duration_ms", duration);
				span.setStatus({ code: SpanStatusCode.OK });
				logger.event()?.setContext("ai-search", {
					index_name: params.indexName,
					query_type: params.queryType,
					result_count: response.result.row_count,
					query_time_ms: response.debug_info?.response_time ?? 0,
					duration_ms: duration
				});
				return response;
			} catch (error) {
				span.recordException(error);
				span.setStatus({
					code: SpanStatusCode.ERROR,
					message: error instanceof Error ? error.message : String(error)
				});
				throw error;
			}
		}, {
			name: "ai-search",
			includePrefix: true
		});
	}
	async queryNextPage(workspaceClient, params, signal) {
		if (signal?.aborted) throw new Error("Query cancelled before execution");
		logger.debug("Fetching next page for index %s (endpoint=%s)", params.indexName, params.endpointName);
		return this.telemetry.startActiveSpan("ai-search.queryNextPage", {
			kind: SpanKind.CLIENT,
			attributes: {
				"db.system": "databricks",
				"vs.index_name": params.indexName,
				"vs.endpoint_name": params.endpointName
			}
		}, async (span) => {
			try {
				const response = await workspaceClient.apiClient.request({
					method: "POST",
					path: `/api/2.0/vector-search/indexes/${params.indexName}/query-next-page`,
					payload: {
						endpoint_name: params.endpointName,
						page_token: params.pageToken
					},
					headers: new Headers({ "Content-Type": "application/json" }),
					raw: false,
					query: {}
				}, contextFromAbortSignal(signal));
				span.setAttribute("vs.result_count", response.result.row_count);
				span.setStatus({ code: SpanStatusCode.OK });
				return response;
			} catch (error) {
				span.recordException(error);
				span.setStatus({
					code: SpanStatusCode.ERROR,
					message: error instanceof Error ? error.message : String(error)
				});
				throw error;
			}
		}, {
			name: "ai-search",
			includePrefix: true
		});
	}
	/**
	* Fetches index metadata (index type, source table). Used to auto-discover
	* returnable columns when they aren't configured. No warehouse required.
	*/
	async getIndex(workspaceClient, indexName, signal) {
		return await workspaceClient.apiClient.request({
			method: "GET",
			path: `/api/2.0/vector-search/indexes/${indexName}`,
			headers: new Headers({ "Content-Type": "application/json" }),
			raw: false,
			query: {}
		}, contextFromAbortSignal(signal));
	}
	/**
	* Lists a Unity Catalog table's column names via the tables REST API
	* (no warehouse required).
	*/
	async getSourceColumns(workspaceClient, sourceTable, signal) {
		return ((await workspaceClient.apiClient.request({
			method: "GET",
			path: `/api/2.1/unity-catalog/tables/${sourceTable}`,
			headers: new Headers({ "Content-Type": "application/json" }),
			raw: false,
			query: {}
		}, contextFromAbortSignal(signal))).columns ?? []).map((c) => c.name);
	}
};

//#endregion
export { AiSearchConnector };
//# sourceMappingURL=client.js.map