//#region src/stream/timers.ts
function clearGraceTimer(entry) {
	if (entry.disconnectGraceTimer) {
		clearTimeout(entry.disconnectGraceTimer);
		entry.disconnectGraceTimer = void 0;
	}
}
function clearRemovalTimer(entry) {
	if (entry.removalTimer) {
		clearTimeout(entry.removalTimer);
		entry.removalTimer = void 0;
	}
}

//#endregion
export { clearGraceTimer, clearRemovalTimer };
//# sourceMappingURL=timers.js.map