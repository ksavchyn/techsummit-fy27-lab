import { SchemaBuildError } from "./types.js";
import { deriveInsertSchema, deriveUpdateSchema } from "./validators.js";
import { ColumnBuilder, enumColumn, validateLiteralDefaults } from "./columns.js";
import { buildEngineTables } from "./engine/tables.js";
import { resolveForeignKeys } from "./fk.js";
import { buildRelations } from "./relations.js";

//#region src/database/schema-builder/define-schema.ts
const RESERVED_OBJECT_KEYS = new Set([
	"__proto__",
	"prototype",
	"constructor"
]);
const RESERVED_DATABASE_EXPORT_KEYS = new Set(["sql", "transaction"]);
const FINALIZED_SCHEMA = Symbol.for("@databricks/appkit.database.schema");
const TABLE_METADATA_KEYS = [
	"$name",
	"$schemaName",
	"$columns",
	"$engine",
	"$relations",
	"$insertSchema",
	"$updateSchema"
];
function nullRecord() {
	return Object.create(null);
}
function assertRecord(value, label) {
	if (value === null || typeof value !== "object" || Array.isArray(value)) throw new SchemaBuildError(`${label} must be an object`);
}
function assertName(value, label) {
	if (!value || RESERVED_OBJECT_KEYS.has(value)) throw new SchemaBuildError(`${label} "${value}" is reserved`);
}
function assertTableName(value) {
	assertName(value, "Table name");
	if (RESERVED_DATABASE_EXPORT_KEYS.has(value)) throw new SchemaBuildError(`Table name "${value}" is reserved`);
}
function finalizeColumn(meta) {
	if (!meta.engineColumn) throw new SchemaBuildError(`Engine column "${meta.columnName}" is missing during finalization`);
	const { fkRef: _fkRef, ...published } = meta;
	return Object.freeze({
		...published,
		enumValues: meta.enumValues ? Object.freeze([...meta.enumValues]) : void 0,
		fk: meta.fk ? Object.freeze({ ...meta.fk }) : void 0,
		engineColumn: meta.engineColumn
	});
}
function finalizeRelations(relations) {
	return Object.freeze(relations.map((relation) => Object.freeze({ ...relation })));
}
/** Clone builders into per-table metadata and create stable column handles. */
function declareTable(state, name, columns) {
	assertTableName(name);
	if (state.raw.has(name)) throw new SchemaBuildError(`Duplicate table "${name}"`);
	assertRecord(columns, `Columns for table "${name}"`);
	const metas = nullRecord();
	const handle = Object.create(null);
	for (const [key, column] of Object.entries(columns)) {
		assertName(key, `Column name on table "${name}"`);
		if (key.startsWith("$") || key === "and" || key === "or") throw new SchemaBuildError(`Column "${name}.${key}" collides with AppKit runtime metadata`);
		if (!(column instanceof ColumnBuilder)) throw new SchemaBuildError(`Column "${name}.${key}" was not created by an AppKit column builder`);
		const meta = column._cloneMeta();
		meta.name = key;
		meta.columnName = key;
		metas[key] = meta;
		const reference = Object.freeze({
			__isColumnRef: true,
			tableName: name,
			columnName: key
		});
		Object.defineProperty(handle, key, {
			enumerable: true,
			value: reference
		});
	}
	state.raw.set(name, {
		name,
		metas,
		handle
	});
	state.handleNames.set(handle, name);
	return handle;
}
function createBuilderContext(state) {
	return {
		table(name, columns) {
			return declareTable(state, name, columns);
		},
		enum(name, values) {
			return enumColumn(name, values);
		}
	};
}
/** Require every exact table handle once under its declared identity. */
function validateReturnedTables(returned, state) {
	assertRecord(returned, "defineSchema() return value");
	const returnedHandles = /* @__PURE__ */ new Set();
	for (const [key, value] of Object.entries(returned)) {
		const name = value !== null && typeof value === "object" ? state.handleNames.get(value) : void 0;
		if (!name) throw new SchemaBuildError(`defineSchema returned a value for "${key}" that was not produced by ctx.table()`);
		if (key !== name) throw new SchemaBuildError(`defineSchema returned table "${name}" under key "${key}"; aliases are not supported`);
		if (returnedHandles.has(value)) throw new SchemaBuildError(`Table "${name}" was returned more than once`);
		returnedHandles.add(value);
	}
	if (returnedHandles.size !== state.raw.size) {
		const omitted = [...state.raw.values()].filter((table) => !returnedHandles.has(table.handle)).map((table) => table.name);
		throw new SchemaBuildError(`defineSchema omitted declared table${omitted.length === 1 ? "" : "s"}: ${omitted.join(", ")}`);
	}
}
/** Detect declaration-handle tampering before metadata publication. */
function validateHandles(raw) {
	for (const table of raw.values()) {
		const ownKeys = Reflect.ownKeys(table.handle);
		const hasOnlyDeclaredColumns = ownKeys.length === Object.keys(table.metas).length && ownKeys.every((key) => typeof key === "string" && Object.hasOwn(table.metas, key));
		const hasMetadataCollision = TABLE_METADATA_KEYS.some((key) => Object.hasOwn(table.handle, key));
		if (Object.getPrototypeOf(table.handle) !== null || !Object.isExtensible(table.handle) || !hasOnlyDeclaredColumns || hasMetadataCollision) throw new SchemaBuildError(`Table handle "${table.name}" was modified during schema declaration`);
	}
}
/** Reject composite primary keys, which keyed operations cannot represent. */
function validatePrimaryKeys(raw) {
	for (const table of raw.values()) if (Object.values(table.metas).filter((meta) => meta.primaryKey).length > 1) throw new SchemaBuildError(`Table "${table.name}" declares multiple primary-key columns; composite primary keys are not supported`);
}
/** Reject table names that collide with generated Drizzle relation keys. */
function validateRelationKeys(raw, relations) {
	for (const [name, tableRelations] of relations) if (tableRelations.length > 0 && raw.has(`${name}Relations`)) throw new SchemaBuildError(`Table "${name}Relations" collides with generated relation metadata for "${name}"`);
}
/** Prepare every engine object and validator without mutating handles. */
function prepareTables(raw, schemaName, relations) {
	const built = buildEngineTables(raw.values(), schemaName);
	const candidates = /* @__PURE__ */ new Map();
	for (const table of raw.values()) {
		const builtTable = built.get(table.name);
		if (!builtTable) throw new SchemaBuildError(`Engine table "${table.name}" was not constructed`);
		const columns = nullRecord();
		for (const [key, meta] of Object.entries(builtTable.columns)) columns[key] = finalizeColumn(meta);
		Object.freeze(columns);
		const tableRelations = finalizeRelations(relations.get(table.name) ?? []);
		const validatorTable = { $columns: columns };
		candidates.set(table.name, {
			columns,
			engine: builtTable.engine,
			relations: tableRelations,
			insertSchema: deriveInsertSchema(validatorTable),
			updateSchema: deriveUpdateSchema(validatorTable)
		});
	}
	return candidates;
}
/** Atomically publish prepared metadata as the final schema transition. */
function publishSchema(raw, candidates, schemaName) {
	const publications = [...raw.values()].map((table) => {
		const candidate = candidates.get(table.name);
		if (!candidate) throw new SchemaBuildError(`Table "${table.name}" was not prepared for finalization`);
		return {
			table,
			candidate
		};
	});
	const tables = nullRecord();
	const engine = nullRecord();
	for (const { table, candidate } of publications) {
		Object.defineProperties(table.handle, {
			$name: { value: table.name },
			$schemaName: { value: schemaName },
			$columns: { value: candidate.columns },
			$engine: { value: candidate.engine },
			$relations: { value: candidate.relations },
			$insertSchema: { value: candidate.insertSchema },
			$updateSchema: { value: candidate.updateSchema }
		});
		const finalized = Object.freeze(table.handle);
		tables[table.name] = finalized;
		engine[table.name] = candidate.engine;
	}
	const schema = {
		$schemaName: schemaName,
		$tables: Object.freeze(tables),
		$engine: Object.freeze(engine)
	};
	Object.defineProperty(schema, FINALIZED_SCHEMA, { value: true });
	return Object.freeze(schema);
}
/** @internal Reject values that were not finalized by `defineSchema()`. */
function assertFinalizedSchema(value) {
	if (value === null || typeof value !== "object" || value[FINALIZED_SCHEMA] !== true) throw new TypeError("Expected a finalized AppKit database schema");
}
function defineSchema(builder, options) {
	const schemaName = options?.schemaName ?? "public";
	if (!schemaName) throw new SchemaBuildError("Schema name cannot be empty");
	const state = {
		raw: /* @__PURE__ */ new Map(),
		handleNames: /* @__PURE__ */ new Map()
	};
	validateReturnedTables(builder(createBuilderContext(state)), state);
	validateHandles(state.raw);
	validatePrimaryKeys(state.raw);
	resolveForeignKeys(state.raw);
	validateLiteralDefaults(state.raw.values());
	const relations = buildRelations(state.raw);
	validateRelationKeys(state.raw, relations);
	const candidates = prepareTables(state.raw, schemaName, relations);
	return publishSchema(state.raw, candidates, schemaName);
}

//#endregion
export { assertFinalizedSchema, defineSchema };
//# sourceMappingURL=define-schema.js.map