import { SchemaBuildError } from "../types.js";
import { relations } from "drizzle-orm";

//#region src/database/schema-builder/engine/relations.ts
function columnOf(table, name) {
	const column = table.$columns[name]?.engineColumn;
	if (!column) throw new SchemaBuildError(`Engine relation column "${table.$name}.${name}" is not finalized`);
	return column;
}
/** Adapt finalized relation metadata to Drizzle's relation registration shape. */
function buildEngineRelations(tables) {
	const byName = /* @__PURE__ */ new Map();
	for (const table of Object.values(tables)) byName.set(table.$name, table);
	const out = Object.create(null);
	for (const table of Object.values(tables)) {
		if (table.$relations.length === 0) continue;
		const localEngine = table.$engine;
		out[`${table.$name}Relations`] = relations(localEngine, ({ one, many }) => {
			const config = Object.create(null);
			for (const relation of table.$relations) {
				const target = byName.get(relation.targetTable);
				if (!target) throw new SchemaBuildError(`Engine relation target "${relation.targetTable}" is not finalized`);
				const targetEngine = target.$engine;
				config[relation.name] = relation.cardinality === "toOne" ? one(targetEngine, {
					fields: [columnOf(table, relation.localColumn)],
					references: [columnOf(target, relation.targetColumn)]
				}) : many(targetEngine);
			}
			return config;
		});
	}
	return out;
}

//#endregion
export { buildEngineRelations };
//# sourceMappingURL=relations.js.map