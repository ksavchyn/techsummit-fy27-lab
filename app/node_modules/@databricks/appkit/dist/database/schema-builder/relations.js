import { SchemaBuildError } from "./types.js";

//#region src/database/schema-builder/relations.ts
/** @internal Build the canonical relation metadata before tables are published. */
function buildRelations(tables) {
	const result = /* @__PURE__ */ new Map();
	for (const name of tables.keys()) result.set(name, []);
	for (const table of tables.values()) {
		const relations = result.get(table.name);
		if (!relations) throw new SchemaBuildError("Relation table is not registered");
		const columnNames = new Set(Object.keys(table.metas));
		const seenTargets = /* @__PURE__ */ new Set();
		for (const meta of Object.values(table.metas)) {
			if (!meta.fk) continue;
			const relationName = meta.fk.targetTable;
			if (columnNames.has(relationName)) throw new SchemaBuildError(`Forward relation "${table.name}.${relationName}" collides with a column of the same name`);
			if (seenTargets.has(relationName)) throw new SchemaBuildError(`Ambiguous forward relation "${table.name}.${relationName}": multiple foreign keys target "${relationName}"`);
			seenTargets.add(relationName);
			relations.push({
				name: relationName,
				cardinality: "toOne",
				localColumn: meta.columnName,
				targetTable: relationName,
				targetColumn: meta.fk.targetColumn,
				inferred: false
			});
		}
	}
	for (const table of tables.values()) for (const meta of Object.values(table.metas)) {
		if (!meta.fk || meta.fk.targetTable === table.name) continue;
		const target = tables.get(meta.fk.targetTable);
		const targetRelations = result.get(meta.fk.targetTable);
		if (!target || !targetRelations) throw new SchemaBuildError(`Relation target "${meta.fk.targetTable}" is not part of the schema`);
		const relationName = table.name;
		if (Object.hasOwn(target.metas, relationName)) throw new SchemaBuildError(`Reverse relation "${target.name}.${relationName}" collides with a column of the same name`);
		if (targetRelations.some((relation) => relation.name === relationName)) throw new SchemaBuildError(`Reverse relation "${target.name}.${relationName}" is ambiguous`);
		targetRelations.push({
			name: relationName,
			cardinality: "toMany",
			localColumn: meta.fk.targetColumn,
			targetTable: table.name,
			targetColumn: meta.columnName,
			inferred: true
		});
	}
	return result;
}

//#endregion
export { buildRelations };
//# sourceMappingURL=relations.js.map