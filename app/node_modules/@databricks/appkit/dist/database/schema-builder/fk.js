import { SchemaBuildError } from "./types.js";
import { ColumnBuilder } from "./columns.js";

//#region src/database/schema-builder/fk.ts
/** Declare foreign-key to another column. */
function fk(ref) {
	const builder = new ColumnBuilder({ kind: "fk" }, "number");
	builder._meta.fkRef = ref;
	return builder;
}
function resolveFkRef(ref) {
	const resolved = typeof ref === "function" ? ref() : ref;
	if (!resolved || typeof resolved !== "object" || resolved.__isColumnRef !== true) throw new SchemaBuildError("fk() must reference a column created by table()");
	return resolved;
}
/** A serial PK target stores as its plain integer type on the FK side. */
function mirrorStorageKind(targetStorage) {
	if (targetStorage === "id") return "integer";
	if (targetStorage === "bigid") return "bigint";
	return targetStorage;
}
/** Resolve FK identity, inherited storage, and action invariants in one pass. */
function resolveForeignKeys(tables) {
	const references = /* @__PURE__ */ new Map();
	for (const table of tables.values()) for (const [columnName, reference] of Object.entries(table.handle)) {
		const meta = table.metas[columnName];
		if (!meta) throw new SchemaBuildError(`Column reference "${table.name}.${columnName}" has no metadata`);
		references.set(reference, {
			table,
			meta
		});
	}
	const resolving = /* @__PURE__ */ new Set();
	const resolved = /* @__PURE__ */ new Set();
	const resolveForeignKey = (table, meta) => {
		if (!meta.fkRef || resolved.has(meta)) return;
		if (resolving.has(meta)) throw new SchemaBuildError(`Foreign key "${table.name}.${meta.columnName}" has a cyclic storage dependency`);
		resolving.add(meta);
		try {
			const reference = resolveFkRef(meta.fkRef);
			const targetIdentity = references.get(reference);
			if (!targetIdentity) throw new SchemaBuildError(`fk() on "${table.name}.${meta.columnName}" targets a column outside the returned schema`);
			const { table: targetTable, meta: target } = targetIdentity;
			resolveForeignKey(targetTable, target);
			if (!target.primaryKey && !target.unique) throw new SchemaBuildError(`fk() on "${table.name}.${meta.columnName}" must target a primary-key or unique column`);
			meta.storageKind = mirrorStorageKind(target.storageKind);
			meta.kind = target.kind;
			meta.withTimezone = target.withTimezone;
			meta.varcharLength = target.varcharLength;
			meta.enumName = target.enumName;
			meta.enumValues = target.enumValues ? Object.freeze([...target.enumValues]) : void 0;
			meta.fk = {
				targetTable: targetTable.name,
				targetColumn: target.columnName,
				onDelete: meta.onDelete,
				onUpdate: meta.onUpdate
			};
			const actions = [meta.onDelete, meta.onUpdate];
			if (actions.includes("set null") && meta.notNull) throw new SchemaBuildError(`Foreign key "${table.name}.${meta.columnName}" uses SET NULL but is not-null`);
			if (actions.includes("set default") && !meta.hasDefault) throw new SchemaBuildError(`Foreign key "${table.name}.${meta.columnName}" uses SET DEFAULT without a local default`);
			resolved.add(meta);
		} finally {
			resolving.delete(meta);
		}
	};
	for (const table of tables.values()) for (const meta of Object.values(table.metas)) resolveForeignKey(table, meta);
}

//#endregion
export { fk, resolveForeignKeys };
//# sourceMappingURL=fk.js.map