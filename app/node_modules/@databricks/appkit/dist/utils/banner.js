//#region src/utils/banner.ts
/**
* Frames content lines in an ASCII box (`===` border, `| … |` sides) padded to
* the widest line. Used for prominent dev-mode warnings that must not be missed
* in a noisy console.
*/
function formatWarningBanner(lines) {
	const maxLen = Math.max(...lines.map((l) => l.length));
	const border = "=".repeat(maxLen + 4);
	return [
		border,
		...lines.map((line) => `| ${line.padEnd(maxLen)} |`),
		border
	].join("\n");
}

//#endregion
export { formatWarningBanner };
//# sourceMappingURL=banner.js.map