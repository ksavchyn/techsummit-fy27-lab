import { filterOperatorsForKind } from "../../database/schema-builder/types.js";

//#region src/type-generator/database/walk-schema.ts
/** Keep generated scalars aligned with the schema's canonical runtime values. */
function tsType(meta) {
	switch (meta.kind) {
		case "string":
		case "uuid":
		case "date": return "string";
		case "number": return "number";
		case "bigint": return "bigint";
		case "boolean": return "boolean";
		case "json": return "unknown";
		case "enum": return meta.enumValues?.map((value) => JSON.stringify(value)).join(" | ") || "string";
		default: return "unknown";
	}
}
function objectFacet(lines, empty = "{}") {
	return lines.length ? `{\n${lines.join("\n")}\n    }` : empty;
}
function property(meta, optional = false) {
	const nullable = meta.notNull ? "" : " | null";
	return `      ${JSON.stringify(meta.columnName)}${optional ? "?" : ""}: ${tsType(meta)}${nullable};`;
}
function rowType(table, publicOnly) {
	return objectFacet(Object.values(table.$columns).filter((c) => !publicOnly || !c.isPrivate).map((c) => property(c)), "Record<string, never>");
}
function insertType(table) {
	return objectFacet(Object.values(table.$columns).filter((c) => !c.serverGenerated).map((c) => property(c, !c.notNull || c.hasDefault)), "Record<string, never>");
}
function updateType(table) {
	return objectFacet(Object.values(table.$columns).filter((c) => !c.serverGenerated && !c.primaryKey).map((c) => property(c, true)), "Record<string, never>");
}
/** Reuse the canonical operator matrix when rendering `where()` types. */
function filtersType(table) {
	return `DatabaseLogicalFilter<${objectFacet(Object.values(table.$columns).flatMap((column) => {
		const operators = filterOperatorsForKind(column.kind);
		if (operators.length === 0) return [];
		const value = tsType(column);
		const fields = operators.map((operator) => `${operator}?: ${operator === "in" ? `readonly (${value})[]` : value};`);
		if (!column.notNull) fields.push("is?: null;");
		return [`      ${JSON.stringify(column.columnName)}?: ${value} | readonly (${value})[] | { ${fields.join(" ")} };`];
	}))}>`;
}
/** Preserve finalized relation identity and cardinality in include types. */
function includesType(table) {
	return objectFacet(table.$relations.map((relation) => `      ${JSON.stringify(relation.name)}: { to: ${JSON.stringify(relation.targetTable)}; many: ${relation.cardinality === "toMany"} };`));
}
/** Preserve schema table identity as the generated registry key. */
function walkSchema(schema) {
	return Object.entries(schema.$tables).map(([name, table]) => ({
		name,
		row: rowType(table, false),
		publicRow: rowType(table, true),
		insert: insertType(table),
		update: updateType(table),
		filters: filtersType(table),
		includes: includesType(table),
		hasPrimaryKey: Object.values(table.$columns).some((column) => column.primaryKey)
	}));
}

//#endregion
export { walkSchema };
//# sourceMappingURL=walk-schema.js.map