import { assertFinalizedSchema } from "../../database/schema-builder/define-schema.js";
import { walkSchema } from "./walk-schema.js";
import { randomUUID } from "node:crypto";
import fs from "node:fs/promises";
import path from "node:path";
import fs$1 from "node:fs";

//#region src/type-generator/database/generate.ts
/** Safe diagnostic raised when database declarations cannot be generated. */
var DatabaseTypegenError = class extends Error {
	constructor(message = "Database schema generation failed") {
		super(message);
		this.name = "DatabaseTypegenError";
	}
};
const DATABASE_TYPES_FILE = "database.d.ts";
function schemaLabel(schemaFile) {
	return path.relative(process.cwd(), schemaFile) || schemaFile;
}
function importFailureReason(error) {
	if (error instanceof SyntaxError) return "could not be parsed";
	let code;
	try {
		code = error && typeof error === "object" ? Reflect.get(error, "code") : void 0;
	} catch {
		code = void 0;
	}
	if (code === "MODULE_NOT_FOUND" || code === "ERR_MODULE_NOT_FOUND") return "contains an unresolved import";
	return "threw while loading";
}
/** Empty augmentation used when no valid database schema is available. */
const NEUTRAL_DATABASE_TYPES = `// Auto-generated by AppKit - DO NOT EDIT
import "@databricks/appkit";

declare module "@databricks/appkit" {
  interface DatabaseRegistry {}
}
`;
/** Render one `DatabaseRegistry` augmentation from a finalized schema. */
function render(schema) {
	return `// Auto-generated by AppKit - DO NOT EDIT
import "@databricks/appkit";

declare module "@databricks/appkit" {
  type DatabaseLogicalFilter<T> = T & {
    and?: readonly DatabaseLogicalFilter<T>[];
    or?: readonly DatabaseLogicalFilter<T>[];
  };

  interface DatabaseRegistry {
${walkSchema(schema).map((entry) => `    ${JSON.stringify(entry.name)}: {
      row: ${entry.row};
      publicRow: ${entry.publicRow};
      insert: ${entry.insert};
      update: ${entry.update};
      filters: ${entry.filters};
      includes: ${entry.includes};
      hasPrimaryKey: ${entry.hasPrimaryKey};
    };`).join("\n")}
  }
}
`;
}
/** Atomically replace generated output only when its content changes. */
async function writeIfChanged(outFile, content) {
	if (fs$1.existsSync(outFile) && await fs.readFile(outFile, "utf8") === content) return;
	await fs.mkdir(path.dirname(outFile), { recursive: true });
	const temporary = path.join(path.dirname(outFile), `.${path.basename(outFile)}.${randomUUID()}.tmp`);
	try {
		await fs.writeFile(temporary, content, "utf8");
		await fs.rename(temporary, outFile);
	} finally {
		await fs.rm(temporary, { force: true });
	}
}
/** Disable Jiti's module cache so watch-mode runs observe schema edits. */
async function importSchema(schemaFile) {
	const { createJiti } = await import("jiti");
	return createJiti(import.meta.url, { moduleCache: false }).import(schemaFile);
}
/** Generate current registry types or neutralize them when loading fails. */
async function generateDatabaseTypes(options) {
	if (!fs$1.existsSync(options.schemaFile)) {
		await writeIfChanged(options.outFile, NEUTRAL_DATABASE_TYPES);
		return;
	}
	try {
		const module = await importSchema(options.schemaFile);
		if (!("schema" in module)) throw new DatabaseTypegenError(`Database schema module "${schemaLabel(options.schemaFile)}" must export a named "schema"`);
		try {
			assertFinalizedSchema(module.schema);
		} catch {
			throw new DatabaseTypegenError(`Database schema "${schemaLabel(options.schemaFile)}" is not a finalized AppKit schema`);
		}
		await writeIfChanged(options.outFile, render(module.schema));
	} catch (error) {
		await writeIfChanged(options.outFile, NEUTRAL_DATABASE_TYPES);
		throw error instanceof DatabaseTypegenError ? error : new DatabaseTypegenError(`Database schema "${schemaLabel(options.schemaFile)}" ${importFailureReason(error)}`);
	}
}

//#endregion
export { DATABASE_TYPES_FILE, DatabaseTypegenError, generateDatabaseTypes };
//# sourceMappingURL=generate.js.map