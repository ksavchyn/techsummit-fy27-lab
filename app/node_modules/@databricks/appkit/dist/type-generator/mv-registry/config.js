import { MAX_UC_OBJECT_NAME_LENGTH, METRIC_CONFIG_FILE, UC_FQN_PATTERN } from "../../shared/src/schemas/metric-fqn.js";
import fs from "node:fs/promises";
import path from "node:path";

//#region src/type-generator/mv-registry/config.ts
/**
* Safety cap on declared metric views — a typo / DoS guard, NOT a Unity Catalog
* limit. Enforced by {@link resolveMetricConfig}.
*/
const MAX_METRIC_VIEWS = 200;
/** Per-segment cap = UC's object-name length limit (255). */
const MAX_FQN_SEGMENT_LENGTH = MAX_UC_OBJECT_NAME_LENGTH;
/** Whole-FQN cap: three max-length segments plus the two separating dots. */
const MAX_FQN_LENGTH = MAX_FQN_SEGMENT_LENGTH * 3 + 2;
/** A metric view FQN is exactly catalog.schema.metric_view. */
const FQN_SEGMENT_NAMES = [
	"catalog",
	"schema",
	"metric_view"
];
const FQN_SEGMENT_COUNT = FQN_SEGMENT_NAMES.length;
/**
* Locale-independent comparator (UTF-16 code-unit order) for metric-view key
* ordering. Plain `sort()` is locale-sensitive, so keys could order differently
* across environments and invalidate the cache hash — this keeps the ordering
* stable everywhere.
*/
function compareKeys(a, b) {
	return a < b ? -1 : a > b ? 1 : 0;
}
/**
* Read {@link METRIC_CONFIG_FILE} from a metric-views folder
* (`config/metric-views/`).
*
* Returns `null` if the file does not exist (the metric-view path is
* additive — apps without definitions.json must not be penalized). There is
* deliberately no fallback to a legacy filename.
*
* Throws on JSON parse errors so misconfiguration surfaces loudly.
*/
async function readMetricConfig(metricViewsFolder) {
	const metricPath = path.join(metricViewsFolder, METRIC_CONFIG_FILE);
	let raw;
	try {
		raw = await fs.readFile(metricPath, "utf8");
	} catch (err) {
		if (err.code === "ENOENT") return null;
		throw err;
	}
	let parsed;
	try {
		parsed = JSON.parse(raw);
	} catch (err) {
		throw new Error(`Failed to parse definitions.json at ${metricPath}: ${err.message}`);
	}
	if (typeof parsed !== "object" || parsed === null || Array.isArray(parsed)) throw new Error(`Invalid definitions.json at ${metricPath}: expected an object with a 'metricViews' map.`);
	return parsed;
}
/**
* Validate a key against the JSON Schema's metricKey pattern. Kept
* lightweight — the shared Zod schema ({@link metricSourceSchema}) is the
* canonical contract for IDE/CI; this regex is identical to its
* {@link metricKeySchema}.
*/
function isValidMetricKey(key) {
	return /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key);
}
/**
* Field allowlists enforced by {@link resolveMetricConfig}.
*/
const ALLOWED_TOP_LEVEL_FIELDS = new Set(["$schema", "metricViews"]);
const ALLOWED_ENTRY_FIELDS = new Set(["source", "executor"]);
/**
* Resolve the {@link MetricSourceConfig.metricViews} map into a flat list of entries.
*/
function resolveMetricConfig(config) {
	for (const field of Object.keys(config)) if (!ALLOWED_TOP_LEVEL_FIELDS.has(field)) throw new Error(`Invalid top-level field "${field}" in definitions.json: only '$schema' and 'metricViews' are allowed.`);
	const metricViews = config.metricViews === void 0 ? {} : config.metricViews;
	if (typeof metricViews !== "object" || metricViews === null || Array.isArray(metricViews)) throw new Error(`Invalid 'metricViews' in definitions.json: expected an object map of metric entries.`);
	const entries = [];
	const sortedKeys = Object.keys(metricViews).sort(compareKeys);
	if (sortedKeys.length > MAX_METRIC_VIEWS) throw new Error(`Invalid 'metricViews' in definitions.json: ${sortedKeys.length} metric views exceed the maximum of ${MAX_METRIC_VIEWS}.`);
	for (const key of sortedKeys) {
		if (!isValidMetricKey(key)) throw new Error(`Invalid metric key "${key}" in metricViews: must match /^[a-zA-Z_][a-zA-Z0-9_]*$/.`);
		const entry = metricViews[key];
		if (!entry || typeof entry !== "object" || Array.isArray(entry)) throw new Error(`Invalid metric entry "${key}": expected an object with a 'source' field.`);
		for (const field of Object.keys(entry)) if (!ALLOWED_ENTRY_FIELDS.has(field)) throw new Error(`Invalid field "${field}" on metric entry "${key}": only 'source' and 'executor' are allowed at v1.`);
		if (typeof entry.source !== "string" || entry.source.trim() === "") throw new Error(`Invalid metric entry "${key}": 'source' must be a non-empty string.`);
		if (entry.source.length > MAX_FQN_LENGTH) throw new Error(`Invalid metric source for "${key}": FQN is ${entry.source.length} characters, exceeding the maximum of ${MAX_FQN_LENGTH}.`);
		const segments = entry.source.split(".");
		if (segments.length !== FQN_SEGMENT_COUNT) throw new Error(`Invalid metric source "${entry.source}" for "${key}": expected a three-part UC FQN <catalog>.<schema>.<metric_view> (got ${segments.length} dot-separated part${segments.length === 1 ? "" : "s"}). A catalog, schema, or metric view name cannot itself contain a dot.`);
		for (let i = 0; i < segments.length; i++) {
			const segment = segments[i];
			const segmentName = FQN_SEGMENT_NAMES[i];
			if (segment.length === 0) throw new Error(`Invalid metric source "${entry.source}" for "${key}": the ${segmentName} part is empty. A three-part UC FQN needs a non-empty name in each position: <catalog>.<schema>.<metric_view>.`);
			if (segment.length > MAX_FQN_SEGMENT_LENGTH) throw new Error(`Invalid metric source for "${key}": the ${segmentName} segment is ${segment.length} characters, exceeding the maximum of ${MAX_FQN_SEGMENT_LENGTH} per segment.`);
			if (!UC_FQN_PATTERN.test(segment)) throw new Error(`Invalid metric source "${entry.source}" for "${key}": the ${segmentName} part "${segment}" contains a character Unity Catalog does not allow in an object name (no spaces, '/', or control characters).`);
		}
		const executor = entry.executor;
		if (executor !== void 0 && executor !== "app_service_principal" && executor !== "user") throw new Error(`Invalid executor "${String(executor)}" on metric entry "${key}": must be "app_service_principal" or "user".`);
		const lane = executor === "user" ? "obo" : "sp";
		entries.push({
			key,
			source: entry.source,
			lane
		});
	}
	return { entries };
}

//#endregion
export { readMetricConfig, resolveMetricConfig };
//# sourceMappingURL=config.js.map