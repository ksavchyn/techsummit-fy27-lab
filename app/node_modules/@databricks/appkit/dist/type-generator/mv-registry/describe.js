import { isValidFqn, quoteFqnForSql } from "../../shared/src/schemas/metric-fqn.js";
import { describeAdaptive } from "../statement-result.js";

//#region src/type-generator/mv-registry/describe.ts
/**
* Parse the JSON payload returned by DESCRIBE TABLE EXTENDED ... AS JSON.
*
* The Statement Execution API returns a single string cell — this normalizer
* unwraps it. Handles both the production (real warehouse) shape and the
* shape produced by mocked test responses.
*/
function parseDescribeTableExtendedJson(response) {
	if (response.status?.state === "FAILED") {
		const msg = response.status.error?.message ?? "DESCRIBE failed";
		throw new Error(`DESCRIBE TABLE EXTENDED failed: ${msg}`);
	}
	const rows = response.result?.data_array ?? [];
	if (rows.length === 0) throw new Error("DESCRIBE TABLE EXTENDED returned no rows. Verify the FQN points to a metric view.");
	const cell = rows[0]?.[0];
	if (typeof cell !== "string") throw new Error("DESCRIBE TABLE EXTENDED first cell was not a JSON string. Confirm the AS JSON suffix is supported.");
	try {
		return JSON.parse(cell);
	} catch (err) {
		throw new Error(`Failed to parse DESCRIBE TABLE EXTENDED JSON: ${err.message}`);
	}
}
/**
* Pure function: turn the parsed DESCRIBE JSON into structured column metadata.
*
* Tolerant of multiple JSON shapes (the field may be `columns` or
* `schema.fields`, type may be a string or `{ name }` object, the measure
* marker may be `is_measure` or under `metadata.is_measure`).
*/
function extractMetricColumns(parsed) {
	if (!parsed || typeof parsed !== "object") return [];
	const root = parsed;
	const columnsCandidate = root.columns ?? (root.schema && typeof root.schema === "object" ? root.schema.fields : void 0);
	if (!Array.isArray(columnsCandidate)) return [];
	const columns = [];
	for (const raw of columnsCandidate) {
		if (!raw || typeof raw !== "object") continue;
		const obj = raw;
		const name = typeof obj.name === "string" ? obj.name : typeof obj.column_name === "string" ? obj.column_name : void 0;
		if (!name) continue;
		const typeRaw = obj.type ?? obj.data_type ?? obj.type_name;
		let type = "STRING";
		if (typeof typeRaw === "string") type = typeRaw;
		else if (typeRaw && typeof typeRaw === "object") {
			const inner = typeRaw.name;
			if (typeof inner === "string") type = inner;
		}
		let isMeasure = false;
		if (typeof obj.is_measure === "boolean") isMeasure = obj.is_measure;
		else if (obj.metadata && typeof obj.metadata === "object" && typeof obj.metadata.is_measure === "boolean") isMeasure = obj.metadata.is_measure;
		else if (obj.kind === "measure" || obj.role === "measure") isMeasure = true;
		const description = typeof obj.comment === "string" ? obj.comment : typeof obj.description === "string" ? obj.description : void 0;
		const displayName = extractStringFromAny(obj, ["display_name", "displayName"]);
		const format = extractFormatString(obj);
		const timeGrains = isMeasure ? void 0 : inferTimeGrains(type);
		columns.push({
			name,
			type,
			isMeasure,
			description,
			...displayName ? { displayName } : {},
			...format ? { format } : {},
			...timeGrains ? { timeGrains } : {}
		});
	}
	return columns;
}
/**
* Read a non-empty string attribute from a DESCRIBE column entry, tolerating
* the multiple shapes UC has shipped for this metadata over time.
*/
function extractStringFromAny(obj, candidates) {
	for (const key of candidates) {
		const direct = obj[key];
		if (typeof direct === "string" && direct.trim().length > 0) return direct;
		const meta = obj.metadata;
		if (meta && typeof meta === "object" && !Array.isArray(meta)) {
			const nested = meta[key];
			if (typeof nested === "string" && nested.trim().length > 0) return nested;
		}
	}
}
/**
* Read the column's `format` attribute from a DESCRIBE entry and return a
* printf-like format string suitable for `formatValue` and `toD3Format`.
*/
function extractFormatString(obj) {
	for (const key of ["format", "format_spec"]) {
		const direct = obj[key];
		const fromDirect = formatStringFromValue(direct);
		if (fromDirect) return fromDirect;
		const meta = obj.metadata;
		if (meta && typeof meta === "object" && !Array.isArray(meta)) {
			const nested = meta[key];
			const fromMeta = formatStringFromValue(nested);
			if (fromMeta) return fromMeta;
		}
	}
}
function formatStringFromValue(value) {
	if (typeof value === "string" && value.trim().length > 0) return value.trim();
	if (value && typeof value === "object" && !Array.isArray(value)) return translateStructuredFormat(value);
}
/**
* Translate the structured `format` object emitted by DESCRIBE TABLE EXTENDED
* AS JSON into a printf-like format string.
*/
function translateStructuredFormat(spec) {
	if (spec.currency && typeof spec.currency === "object") return currencyFormatString(spec.currency);
	if (spec.percent && typeof spec.percent === "object") return percentFormatString(spec.percent);
	if (spec.number && typeof spec.number === "object") return numberFormatString(spec.number);
}
function currencyFormatString(c) {
	const places = readDecimalPlaces(c) ?? 2;
	const codeRaw = c.currency_code;
	return `${currencySymbol(typeof codeRaw === "string" && codeRaw.trim().length > 0 ? codeRaw.toUpperCase() : "USD")}#,##0${fractionalSuffix(places)}`;
}
function percentFormatString(p) {
	return `0${fractionalSuffix(readDecimalPlaces(p) ?? 0)}%`;
}
function numberFormatString(n) {
	return `#,##0${fractionalSuffix(readDecimalPlaces(n) ?? 0)}`;
}
function fractionalSuffix(places) {
	return places > 0 ? `.${"0".repeat(places)}` : "";
}
/**
* Maximum decimal places honored from a format spec.
*/
const MAX_DECIMAL_PLACES = 100;
function readDecimalPlaces(obj) {
	const dp = obj.decimal_places;
	if (typeof dp === "number" && Number.isFinite(dp) && dp >= 0) return Math.min(Math.floor(dp), MAX_DECIMAL_PLACES);
	if (dp && typeof dp === "object" && !Array.isArray(dp)) {
		const places = dp.places;
		if (typeof places === "number" && Number.isFinite(places) && places >= 0) return Math.min(Math.floor(places), MAX_DECIMAL_PLACES);
	}
}
/**
* Map ISO currency codes to their conventional prefix symbol. Unknown codes
* fall back to the literal code + space.
*/
const CURRENCY_SYMBOLS = {
	USD: "$",
	EUR: "€",
	GBP: "£",
	JPY: "¥",
	CNY: "¥",
	INR: "₹",
	BRL: "R$"
};
function currencySymbol(code) {
	return CURRENCY_SYMBOLS[code] ?? `${code} `;
}
/**
* Infer the standard set of valid time grains for a dimension based on its
* SQL data type.
*/
function inferTimeGrains(type) {
	const normalized = type.toLowerCase().replace(/\(.*\)$/, "").trim();
	if (normalized === "timestamp" || normalized === "timestamp_ltz" || normalized === "timestamp_ntz") return [
		"day",
		"hour",
		"minute",
		"month",
		"quarter",
		"week",
		"year"
	];
	if (normalized === "date") return [
		"day",
		"month",
		"quarter",
		"week",
		"year"
	];
}
/**
* Build a DescribeFetcher from a real WorkspaceClient + warehouseId.
*/
function createWorkspaceDescribeFetcher(client, warehouseId) {
	const describeFormat = {};
	return async (fqn) => {
		/**
		* Defense-in-depth: every caller passes a source that already cleared
		* {@link resolveMetricConfig}, but this fetcher is an exported seam — re-check
		* before interpolating into SQL.
		*/
		if (!isValidFqn(fqn)) throw new Error(`Invalid metric source "${fqn}": expected a three-part UC FQN <catalog>.<schema>.<metric_view>.`);
		return describeAdaptive(client, `DESCRIBE TABLE EXTENDED ${quoteFqnForSql(fqn)} AS JSON`, warehouseId, describeFormat);
	};
}

//#endregion
export { createWorkspaceDescribeFetcher, extractMetricColumns, parseDescribeTableExtendedJson };
//# sourceMappingURL=describe.js.map