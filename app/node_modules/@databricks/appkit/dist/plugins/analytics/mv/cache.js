import { AuthenticationError } from "../../../errors/authentication.js";
import "../../../errors/index.js";
import { sortFilterChildren } from "./formatters.js";
import { createHash } from "node:crypto";

//#region src/plugins/analytics/mv/cache.ts
function composeMetricCacheKey(input) {
	const sortedMeasures = [...input.measures].sort();
	const sortedDimensions = [...input.dimensions ?? []].sort();
	const filterFingerprint = input.filter !== void 0 ? canonicalizeFilter(input.filter) : "_";
	const timeDimensionPart = input.timeGrain != null ? input.timeDimension ?? "_" : "_";
	const orderByPart = input.orderBy !== void 0 && input.orderBy.length > 0 ? JSON.stringify(input.orderBy.map((o) => [o.field, o.direction ?? "ASC"])) : "_";
	return [
		"metric",
		input.metricKey,
		input.source,
		input.format,
		JSON.stringify(sortedMeasures),
		JSON.stringify(sortedDimensions),
		input.timeGrain ?? "_",
		timeDimensionPart,
		filterFingerprint,
		typeof input.limit === "number" ? String(input.limit) : "_",
		orderByPart,
		input.executorKey
	];
}
function deriveMetricExecutorKey(input) {
	if (input.lane === "sp") return "sp";
	const identity = input.userIdentity?.trim();
	if (!identity) throw AuthenticationError.missingUserId();
	return createHash("sha256").update(identity).digest("hex");
}
function canonicalizeFilter(node) {
	if (node === null || typeof node !== "object") return "_";
	if ("and" in node || "or" in node) {
		const groupKey = "and" in node ? "and" : "or";
		const children = node[groupKey];
		if (!Array.isArray(children) || children.length === 0) return `${groupKey}()`;
		return `${groupKey}(${sortFilterChildren(children).map(canonicalizeFilter).join(",")})`;
	}
	const p = node;
	const valuesPart = (p.values ?? []).map((v) => [typeof v, v]);
	return `p(${JSON.stringify([
		p.member,
		p.operator,
		valuesPart
	])})`;
}

//#endregion
export { composeMetricCacheKey, deriveMetricExecutorKey };
//# sourceMappingURL=cache.js.map