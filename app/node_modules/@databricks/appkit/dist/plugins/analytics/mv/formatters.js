import { sql } from "../../../shared/src/sql/helpers.js";
import { isValidColumnName, isValidFqn, quoteFqnForSql, quoteIdentifier } from "../../../shared/src/schemas/metric-fqn.js";
import { METRIC_FILTER_MAX_DEPTH, METRIC_FILTER_OPERATORS, TIME_GRAIN_PATTERN } from "./constants.js";

//#region src/plugins/analytics/mv/formatters.ts
function quoteSafeFqn(fqn) {
	if (!isValidFqn(fqn)) throw new Error(`Refusing to build SQL: "${fqn}" is not a valid three-part UC FQN.`);
	return quoteFqnForSql(fqn);
}
function buildMetricSql(registration, request) {
	const quotedSource = quoteSafeFqn(registration.source);
	if (request.measures.length === 0) throw new Error("buildMetricSql requires at least one measure.");
	for (const m of request.measures) if (!isValidColumnName(m)) throw new Error(`Refusing to build SQL: measure "${m}" is not a valid identifier.`);
	const dimensions = request.dimensions ?? [];
	for (const d of dimensions) if (!isValidColumnName(d)) throw new Error(`Refusing to build SQL: dimension "${d}" is not a valid identifier.`);
	const measureClauses = [...request.measures].sort().map((m) => `MEASURE(${quoteIdentifier(m)}) AS ${quoteIdentifier(m)}`);
	const sortedDimensions = [...dimensions].sort();
	const dimensionClauses = sortedDimensions.map((d) => renderDimensionClause(d, request.timeGrain, request.timeDimension));
	const selectList = [...measureClauses, ...dimensionClauses].join(", ");
	const groupByClause = dimensions.length > 0 ? " GROUP BY ALL" : "";
	const orderByClause = renderOrderByClause(request, sortedDimensions);
	const limitClause = typeof request.limit === "number" && request.limit > 0 ? ` LIMIT ${Math.floor(request.limit)}` : "";
	const parameters = {};
	let whereClause = "";
	if (request.filter !== void 0) {
		const fragment = renderFilter(request.filter, parameters, {
			counter: 0,
			depth: 0
		});
		if (fragment !== null && fragment.length > 0) whereClause = ` WHERE ${fragment}`;
	}
	return {
		statement: `SELECT ${selectList} FROM ${quotedSource}${whereClause}${groupByClause}${orderByClause}${limitClause}`,
		parameters
	};
}
function renderFilter(node, params, state) {
	if (node === null || typeof node !== "object") throw new Error("Refusing to build SQL: filter node must be an object Predicate or { and } / { or } group.");
	if ("and" in node || "or" in node) {
		const groupKey = "and" in node ? "and" : "or";
		if (state.depth + 1 > METRIC_FILTER_MAX_DEPTH) throw new Error(`Refusing to build SQL: filter AND/OR nesting exceeds the maximum depth of ${METRIC_FILTER_MAX_DEPTH}.`);
		const children = node[groupKey];
		if (!Array.isArray(children) || children.length === 0) return groupKey === "or" ? "1 = 0" : "1 = 1";
		const sortedChildren = sortFilterChildren(children);
		const fragments = [];
		const childState = {
			counter: state.counter,
			depth: state.depth + 1
		};
		for (const child of sortedChildren) {
			const rendered = renderFilter(child, params, childState);
			if (rendered != null && rendered.length > 0) fragments.push(rendered);
		}
		state.counter = childState.counter;
		if (fragments.length === 0) return null;
		if (fragments.length === 1) return fragments[0];
		const joiner = groupKey === "and" ? " AND " : " OR ";
		return `(${fragments.join(joiner)})`;
	}
	const predicate = node;
	if (!isValidColumnName(predicate.member)) throw new Error(`Refusing to build SQL: filter member "${predicate.member}" is not a valid identifier.`);
	if (!METRIC_FILTER_OPERATORS.includes(predicate.operator)) throw new Error(`Refusing to build SQL: unknown filter operator "${predicate.operator}".`);
	return renderPredicate(predicate, params, state);
}
function sortFilterChildren(children) {
	const indexed = children.map((child, idx) => {
		let key;
		let isPredicate;
		if (child !== null && typeof child === "object" && !("and" in child) && !("or" in child)) {
			const p = child;
			key = JSON.stringify([p.member, p.operator]);
			isPredicate = true;
		} else {
			key = "";
			isPredicate = false;
		}
		return {
			child,
			idx,
			key,
			isPredicate
		};
	});
	indexed.sort((a, b) => {
		if (a.isPredicate && !b.isPredicate) return -1;
		if (!a.isPredicate && b.isPredicate) return 1;
		if (a.isPredicate && b.isPredicate) {
			if (a.key < b.key) return -1;
			if (a.key > b.key) return 1;
		}
		return a.idx - b.idx;
	});
	return indexed.map((entry) => entry.child);
}
function renderPredicate(predicate, params, state) {
	const col = quoteIdentifier(predicate.member);
	const op = predicate.operator;
	const values = predicate.values ?? [];
	switch (op) {
		case "equals": return `${col} = ${bindValue(values[0], params, state)}`;
		case "notEquals": return `${col} <> ${bindValue(values[0], params, state)}`;
		case "gt": return `${col} > ${bindValue(values[0], params, state)}`;
		case "gte": return `${col} >= ${bindValue(values[0], params, state)}`;
		case "lt": return `${col} < ${bindValue(values[0], params, state)}`;
		case "lte": return `${col} <= ${bindValue(values[0], params, state)}`;
		case "in": return `${col} IN (${values.map((v) => bindValue(v, params, state)).join(", ")})`;
		case "notIn": return `${col} NOT IN (${values.map((v) => bindValue(v, params, state)).join(", ")})`;
		case "contains": {
			const raw = values[0];
			if (typeof raw !== "string") throw new Error(`Refusing to build SQL: filter operator "contains" requires a string value (got ${typeof raw}).`);
			return `${col} LIKE ${bindLikeValue(raw, params, state)}`;
		}
		case "notContains": {
			const raw = values[0];
			if (typeof raw !== "string") throw new Error(`Refusing to build SQL: filter operator "notContains" requires a string value (got ${typeof raw}).`);
			return `${col} NOT LIKE ${bindLikeValue(raw, params, state)}`;
		}
		case "set": return `${col} IS NOT NULL`;
		case "notSet": return `${col} IS NULL`;
		default: {
			const _exhaustive = op;
			throw new Error(`Refusing to build SQL: unhandled filter operator "${_exhaustive}".`);
		}
	}
}
function bindValue(value, params, state) {
	if (value === void 0) throw new Error("Refusing to build SQL: filter predicate is missing a required value.");
	const name = `f_${state.counter}`;
	state.counter += 1;
	if (typeof value === "number") params[name] = sql.number(value);
	else if (typeof value === "string") params[name] = sql.string(value);
	else throw new Error(`Refusing to build SQL: filter value must be a string or number (got ${typeof value}).`);
	return `:${name}`;
}
function bindLikeValue(value, params, state) {
	const name = `f_${state.counter}`;
	state.counter += 1;
	params[name] = sql.string(`%${value}%`);
	return `:${name}`;
}
function renderDimensionClause(dim, timeGrain, timeDimension) {
	if (timeGrain != null && dim === timeDimension) {
		if (!isValidColumnName(dim)) throw new Error(`Refusing to build SQL: timeDimension "${dim}" is not a valid identifier.`);
		if (!TIME_GRAIN_PATTERN.test(timeGrain)) throw new Error(`Refusing to build SQL: timeGrain "${timeGrain}" is not a valid grain token.`);
		const quoted = quoteIdentifier(dim);
		return `date_trunc('${timeGrain}', ${quoted}) AS ${quoted}`;
	}
	return quoteIdentifier(dim);
}
function renderOrderByClause(request, sortedDimensions) {
	const keyList = [];
	if (request.orderBy != null && request.orderBy.length > 0) for (const entry of request.orderBy) {
		if (!isValidColumnName(entry.field)) throw new Error(`Refusing to build SQL: orderBy field "${entry.field}" is not a valid identifier.`);
		const direction = entry.direction === "DESC" ? " DESC" : "";
		keyList.push(`${quoteIdentifier(entry.field)}${direction}`);
	}
	if (typeof request.limit === "number" && request.limit > 0) {
		const orderByFields = new Set(request.orderBy?.map((e) => e.field) ?? []);
		for (const dim of sortedDimensions) if (!orderByFields.has(dim)) keyList.push(quoteIdentifier(dim));
	}
	if (keyList.length === 0) return "";
	return ` ORDER BY ${keyList.join(", ")}`;
}

//#endregion
export { buildMetricSql, sortFilterChildren };
//# sourceMappingURL=formatters.js.map