import { Plugin } from "../../plugin/plugin.js";
import "../../plugin/index.js";
import { DatabasePluginError } from "../../database/errors.js";
import { createDatabaseState } from "./lifecycle.js";
import manifest_default from "./manifest.js";

//#region src/plugins/database/database.ts
/** Schema-driven database plugin */
var DatabasePlugin = class extends Plugin {
	/** Plugin metadata and required PostgreSQL resource. */
	static manifest = manifest_default;
	state = null;
	setupPromise = null;
	draining = false;
	shutdownPromise = null;
	constructor(config) {
		super({ schema: config.schema });
		this.config = { schema: config.schema };
	}
	/** Build and verify one candidate state before publishing its exports. */
	async setup() {
		if (this.draining || this.state) throw new DatabasePluginError("SETUP_FAILED", "setup");
		if (!this.setupPromise) this.setupPromise = (async () => {
			const candidate = await createDatabaseState(this.config.schema, (operation, options) => this.execute(operation, options));
			if (this.draining) {
				candidate.deactivate();
				await candidate.pool.end().catch(() => void 0);
				throw new DatabasePluginError("SETUP_FAILED", "setup");
			}
			this.state = candidate;
		})();
		return this.setupPromise;
	}
	/** Return the typed database API only while the plugin is active. */
	exports() {
		if (!this.state || this.draining) throw new DatabasePluginError("INTERNAL", "read");
		return Object.assign(Object.create(null), this.state.exports);
	}
	/** Stop new work, wait for setup, and close the owned pool exactly once. */
	async shutdown() {
		if (this.shutdownPromise) return this.shutdownPromise;
		this.draining = true;
		this.shutdownPromise = (async () => {
			await this.setupPromise?.catch(() => void 0);
			const state = this.state;
			state?.deactivate();
			this.state = null;
			if (state) try {
				await state.pool.end();
			} catch {
				throw new DatabasePluginError("INTERNAL", "shutdown");
			}
		})();
		return this.shutdownPromise;
	}
};
/** Create a typed database plugin registration for a finalized schema. */
function database(config) {
	return {
		plugin: DatabasePlugin,
		config,
		name: "database"
	};
}

//#endregion
export { database };
//# sourceMappingURL=database.js.map