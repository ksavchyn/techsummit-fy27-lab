import { createLogger } from "../../logging/logger.js";
import { getCurrentUserId, getWorkspaceClient } from "../../context/execution-context.js";
import { formatWarningBanner } from "../../utils/banner.js";
import "../../context/index.js";
import { Plugin } from "../../plugin/plugin.js";
import { toPlugin } from "../../plugin/to-plugin.js";
import "../../plugin/index.js";
import { defineManifest } from "../../registry/manifest-loader.js";
import "../../registry/index.js";
import { AiSearchConnector } from "../../connectors/ai-search/client.js";
import { aiSearchDefaults } from "./defaults.js";
import manifest_default from "./manifest.js";
import { createHash } from "node:crypto";

//#region src/plugins/ai-search/ai-search.ts
const logger = createLogger("ai-search");
const querySettings = { default: aiSearchDefaults };
var AiSearchPlugin = class extends Plugin {
	static manifest = defineManifest(manifest_default);
	static description = "Query Databricks Vector Search indexes with hybrid search, reranking, and pagination";
	connector;
	constructor(config) {
		super(config);
		this.config = {
			...config,
			indexes: config.indexes ?? this._defaultIndexes()
		};
		this.connector = new AiSearchConnector({
			timeout: config.timeout,
			telemetry: config.telemetry
		});
	}
	/**
	* Seeds a `default` index from `DATABRICKS_VS_INDEX_NAME` when no `indexes`
	* are configured, so `aiSearch()` works with just the env var.
	*/
	_defaultIndexes() {
		const indexName = process.env.DATABRICKS_VS_INDEX_NAME;
		return indexName ? { default: { indexName } } : {};
	}
	async setup() {
		for (const [alias, idx] of Object.entries(this.config.indexes ?? {})) if (idx.pagination && !idx.endpointName) throw new Error(`Index "${alias}" has pagination enabled but is missing "endpointName"`);
		if (process.env.NODE_ENV === "development") await this._autoDiscoverColumns();
		else for (const [alias, idx] of Object.entries(this.config.indexes ?? {})) if (!idx.columns || idx.columns.length === 0) throw new Error(`Index "${alias}" has no columns configured. Vector Search queries require "columns"; set them explicitly (auto-discovered only in development).`);
	}
	/**
	* For each configured index missing `columns`, fill them from its Delta-Sync
	* source table (all source columns minus embedding vectors). Best-effort:
	* failures are logged and skipped, never thrown. A partial `columns_to_sync`
	* isn't honored, so the discovered list is a starting point to trim.
	*/
	async _autoDiscoverColumns() {
		const discovered = {};
		for (const [alias, idx] of Object.entries(this.config.indexes ?? {})) {
			if (idx.columns && idx.columns.length > 0) continue;
			const indexName = idx.indexName ?? process.env.DATABRICKS_VS_INDEX_NAME;
			if (!indexName) continue;
			try {
				const client = getWorkspaceClient();
				const info = await this.connector.getIndex(client, indexName);
				const sourceTable = info.delta_sync_index_spec?.source_table;
				if (!sourceTable) continue;
				const excluded = new Set((info.delta_sync_index_spec?.embedding_vector_columns ?? []).map((c) => c.name));
				const columns = (await this.connector.getSourceColumns(client, sourceTable)).filter((c) => !excluded.has(c));
				if (columns.length > 0) {
					idx.columns = columns;
					discovered[alias] = columns;
				}
			} catch (error) {
				logger.warn("Could not auto-discover columns for index \"%s\": %s", alias, error instanceof Error ? error.message : String(error));
			}
		}
		if (Object.keys(discovered).length > 0) logger.warn("\n%s", this._formatColumnDiscoveryBanner(discovered));
	}
	_formatColumnDiscoveryBanner(discovered) {
		const lines = ["AI SEARCH: columns auto-discovered (dev mode — would fail in production)", ""];
		for (const [alias, columns] of Object.entries(discovered)) lines.push(`  ${alias}: ${columns.join(", ")}`);
		lines.push("");
		lines.push("Set `columns` explicitly in the plugin config before deploying.");
		return formatWarningBanner(lines);
	}
	injectRoutes(router) {
		this.route(router, {
			name: "query",
			method: "post",
			path: "/:alias/query",
			handler: async (req, res) => {
				const indexConfig = this._resolveOr404(req, res);
				if (!indexConfig) return;
				const body = req.body;
				if (!body.queryText && !body.queryVector) {
					res.status(400).json({
						error: "queryText or queryVector is required",
						plugin: this.name
					});
					return;
				}
				const { columns: _clientColumns, ...safeBody } = body;
				const isAsUser = indexConfig.auth === "on-behalf-of-user";
				const plugin = isAsUser ? this.asUser(req) : this;
				const queryType = safeBody.queryType ?? indexConfig.queryType ?? "hybrid";
				const executorKey = isAsUser ? this.resolveUserId(req) : "global";
				try {
					const result = await plugin.execute(async (signal) => {
						const prepared = await this._prepareQuery(safeBody, indexConfig);
						return this.connector.query(getWorkspaceClient(), {
							indexName: indexConfig.indexName,
							...prepared
						}, signal);
					}, this._executeSettings(safeBody, indexConfig, executorKey));
					this._sendResult(res, result, queryType);
				} catch (error) {
					this._handleError(res, error, "Query failed");
				}
			}
		});
		this.route(router, {
			name: "queryNextPage",
			method: "post",
			path: "/:alias/next-page",
			handler: async (req, res) => {
				const indexConfig = this._resolveOr404(req, res);
				if (!indexConfig) return;
				if (!indexConfig.pagination) {
					res.status(400).json({
						error: `Pagination is not enabled for index "${req.params.alias}"`,
						plugin: this.name
					});
					return;
				}
				if (!indexConfig.endpointName) {
					res.status(400).json({
						error: `Index "${req.params.alias}" is missing endpointName required for pagination`,
						plugin: this.name
					});
					return;
				}
				const { pageToken, queryType } = req.body;
				if (!pageToken) {
					res.status(400).json({
						error: "pageToken is required",
						plugin: this.name
					});
					return;
				}
				const pageQueryType = queryType ?? indexConfig.queryType ?? "hybrid";
				try {
					const result = await (indexConfig.auth === "on-behalf-of-user" ? this.asUser(req) : this).execute(async (signal) => this.connector.queryNextPage(getWorkspaceClient(), {
						indexName: indexConfig.indexName,
						endpointName: indexConfig.endpointName,
						pageToken
					}, signal), querySettings);
					this._sendResult(res, result, pageQueryType);
				} catch (error) {
					this._handleError(res, error, "Next-page query failed");
				}
			}
		});
		this.route(router, {
			name: "getConfig",
			method: "get",
			path: "/:alias/config",
			handler: async (req, res) => {
				const indexConfig = this._resolveOr404(req, res);
				if (!indexConfig) return;
				res.json({
					alias: req.params.alias,
					columns: indexConfig.columns,
					queryType: indexConfig.queryType ?? "hybrid",
					numResults: indexConfig.numResults ?? 20,
					reranker: !!indexConfig.reranker,
					pagination: !!indexConfig.pagination
				});
			}
		});
	}
	/**
	* Index aliases + non-sensitive query metadata, serialized to the client so
	* the UI can discover available indexes instead of hardcoding an alias.
	*/
	clientConfig() {
		return { indexes: Object.entries(this.config.indexes ?? {}).map(([alias, idx]) => ({
			alias,
			queryType: idx.queryType ?? "hybrid",
			pagination: !!idx.pagination
		})) };
	}
	/**
	* Programmatic query API — available as `appkit.aiSearch.query()`.
	* When called through `asUser(req)`, executes with the user's credentials.
	*
	* @remarks `T` types each result's `data` but is an unchecked cast — the row
	* shape isn't validated at runtime.
	*/
	async query(alias, request) {
		const indexConfig = this._resolveIndex(alias);
		if (!indexConfig) throw new Error(`No index configured with alias "${alias}"`);
		const { queryType } = this._resolveQueryParams(request, indexConfig);
		const result = await this.execute(async (signal) => {
			const prepared = await this._prepareQuery(request, indexConfig);
			return this.connector.query(getWorkspaceClient(), {
				indexName: indexConfig.indexName,
				...prepared
			}, signal);
		}, this._executeSettings(request, indexConfig, getCurrentUserId()));
		if (!result.ok) throw new Error(`Vector search query failed for index "${alias}": ${result.message}`);
		return this._parseResponse(result.data, queryType);
	}
	async shutdown() {}
	exports() {
		return { query: this.query.bind(this) };
	}
	_resolveIndex(alias) {
		const idx = this.config.indexes?.[alias];
		if (!idx) return void 0;
		const indexName = idx.indexName ?? process.env.DATABRICKS_VS_INDEX_NAME;
		if (!indexName) return void 0;
		return {
			...idx,
			indexName
		};
	}
	/** Resolve an index by route alias, or send a 404 and return null. */
	_resolveOr404(req, res) {
		const indexConfig = this._resolveIndex(req.params.alias);
		if (!indexConfig) {
			res.status(404).json({
				error: `No index configured with alias "${req.params.alias}"`,
				plugin: this.name
			});
			return null;
		}
		return indexConfig;
	}
	/** Send an execution result as JSON, or its error status/message. */
	_sendResult(res, result, queryType) {
		if (!result.ok) {
			res.status(result.status).json({
				error: result.message,
				plugin: this.name
			});
			return;
		}
		res.json(this._parseResponse(result.data, queryType));
	}
	/**
	* Resolve request-vs-index defaults for the result-determining fields.
	* Shared by `_prepareQuery` (the payload) and `_cacheKeyFor` (the key) so
	* the two can't drift.
	*/
	_resolveQueryParams(request, indexConfig) {
		const queryType = request.queryType ?? indexConfig.queryType ?? "hybrid";
		const columns = request.columns ?? indexConfig.columns ?? [];
		return {
			queryType,
			columns,
			numResults: request.numResults ?? indexConfig.numResults ?? 20,
			reranker: this._resolveReranker(request.reranker, indexConfig, columns)
		};
	}
	async _prepareQuery(request, indexConfig) {
		const { queryType, columns, numResults, reranker } = this._resolveQueryParams(request, indexConfig);
		let queryText = request.queryText;
		let queryVector = request.queryVector;
		if (indexConfig.embeddingFn && queryText && !queryVector && queryType !== "full_text") try {
			queryVector = await indexConfig.embeddingFn(queryText);
			if (queryType === "ann") queryText = void 0;
		} catch (error) {
			throw new Error(`Embedding generation failed: ${error instanceof Error ? error.message : String(error)}`);
		}
		return {
			queryText,
			queryVector,
			queryType,
			columns,
			numResults,
			filters: request.filters,
			reranker
		};
	}
	/**
	* Cache key for a query: every input that changes the VS result, resolved
	* via `_resolveQueryParams` so the key matches the payload (post-allowlist
	* `columns`, not the raw request). `queryVector` is hashed (vectors are
	* large); the key uses `queryText`, not the derived embedding, since the
	* embedding is a function of `queryText` and hasn't run at key-build time.
	* `columns`/`filters` are order-normalized so equivalent requests share an
	* entry.
	*/
	_cacheKeyFor(request, indexConfig, executorKey) {
		const { queryType, columns, numResults, reranker } = this._resolveQueryParams(request, indexConfig);
		return [
			"ai-search:query",
			indexConfig.indexName,
			request.queryText ?? "",
			request.queryVector ? this._hashVector(request.queryVector) : "",
			queryType,
			numResults,
			JSON.stringify([...columns].sort()),
			this._stableStringify(request.filters ?? null),
			String(!!reranker),
			executorKey
		];
	}
	_hashVector(vector) {
		return createHash("sha256").update(JSON.stringify(vector)).digest("hex");
	}
	/** Execute settings with the per-call cache key folded in. */
	_executeSettings(request, indexConfig, executorKey) {
		return { default: {
			...aiSearchDefaults,
			cache: {
				...aiSearchDefaults.cache,
				cacheKey: this._cacheKeyFor(request, indexConfig, executorKey)
			}
		} };
	}
	/** `JSON.stringify` with object keys sorted recursively; array order kept. */
	_stableStringify(value) {
		return JSON.stringify(value, (_key, val) => val && typeof val === "object" && !Array.isArray(val) ? Object.fromEntries(Object.entries(val).sort(([a], [b]) => a.localeCompare(b))) : val);
	}
	_resolveReranker(requestReranker, indexConfig, columns) {
		if (!(requestReranker ?? indexConfig.reranker)) return void 0;
		if (typeof indexConfig.reranker === "object") return indexConfig.reranker;
		const columnsToRerank = columns.filter((c) => c !== "id");
		return columnsToRerank.length > 0 ? { columnsToRerank } : void 0;
	}
	_parseResponse(raw, queryType) {
		const columnNames = raw.manifest.columns.map((c) => c.name);
		const scoreIndex = columnNames.indexOf("score");
		return {
			results: raw.result.data_array.map((row) => {
				const data = {};
				for (let i = 0; i < columnNames.length; i++) if (i !== scoreIndex) data[columnNames[i]] = row[i];
				return {
					score: scoreIndex >= 0 ? row[scoreIndex] : 0,
					data
				};
			}),
			totalCount: raw.result.row_count,
			queryTimeMs: raw.debug_info?.response_time ?? raw.debug_info?.latency_ms ?? 0,
			queryType,
			nextPageToken: raw.next_page_token ?? null
		};
	}
	_handleError(res, error, fallbackMessage) {
		logger.error("%s: %O", fallbackMessage, error);
		const message = process.env.NODE_ENV !== "production" && error instanceof Error ? error.message : fallbackMessage;
		res.status(500).json({
			error: message,
			plugin: this.name
		});
	}
};
const aiSearch = toPlugin(AiSearchPlugin);

//#endregion
export { AiSearchPlugin, aiSearch };
//# sourceMappingURL=ai-search.js.map