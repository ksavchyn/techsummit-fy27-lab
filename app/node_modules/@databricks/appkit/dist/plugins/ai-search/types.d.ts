import { BasePluginConfig } from "../../shared/src/plugin.js";
import "../../shared/src/index.js";

//#region src/plugins/ai-search/types.d.ts
/** Vector Search query mode: semantic (`ann`), keyword+semantic (`hybrid`), or keyword-only (`full_text`). */
type SearchQueryType = "ann" | "hybrid" | "full_text";
interface IAiSearchConfig extends BasePluginConfig {
  timeout?: number;
  indexes?: Record<string, IndexConfig>;
}
interface IndexConfig {
  /**
   * Three-level UC name: catalog.schema.index_name. Defaults to the
   * `DATABRICKS_VS_INDEX_NAME` env var when omitted — so multiple aliases that
   * omit it all resolve to that same physical index. Set it explicitly per
   * alias when they should point at distinct indexes.
   */
  indexName?: string;
  /**
   * Columns to return in results. Optional: in development the plugin
   * auto-discovers them from the index's source table when omitted (and warns
   * that they should be set explicitly for production).
   */
  columns?: string[];
  /** Default search mode */
  queryType?: SearchQueryType;
  /** Max results per query */
  numResults?: number;
  /** Enable built-in reranker. Pass true to rerank all non-id columns, or an object for fine control. */
  reranker?: boolean | RerankerConfig;
  /**
   * Auth mode for the built-in HTTP routes — "service-principal" (default)
   * uses the app's SP, "on-behalf-of-user" proxies the logged-in user's token.
   * Programmatic callers select per call via `appkit.aiSearch.asUser(req)`.
   */
  auth?: "service-principal" | "on-behalf-of-user";
  /** Enable cursor pagination */
  pagination?: boolean;
  /** VS endpoint name (required when pagination is true) */
  endpointName?: string;
  /**
   * For self-managed embedding indexes: converts query text to an embedding vector.
   * When provided, the plugin calls this function and sends query_vector to VS.
   * When omitted, query_text is sent and VS computes embeddings server-side (managed mode).
   */
  embeddingFn?: (text: string) => Promise<number[]>;
}
interface RerankerConfig {
  columnsToRerank: string[];
}
/** Public summary of a configured index, exposed to the client via `clientConfig()`. */
interface IndexSummary {
  alias: string;
  queryType: SearchQueryType;
  pagination: boolean;
}
type SearchFilters = Record<string, string | number | boolean | (string | number)[]>;
interface SearchRequest {
  queryText?: string;
  queryVector?: number[];
  columns?: string[];
  numResults?: number;
  queryType?: SearchQueryType;
  filters?: SearchFilters;
  reranker?: boolean;
}
interface SearchResponse<T extends Record<string, unknown> = Record<string, unknown>> {
  results: SearchResult<T>[];
  totalCount: number;
  queryTimeMs: number;
  queryType: SearchQueryType;
  nextPageToken: string | null;
}
interface SearchResult<T extends Record<string, unknown> = Record<string, unknown>> {
  score: number;
  data: T;
}
//#endregion
export { IAiSearchConfig, IndexConfig, IndexSummary, RerankerConfig, SearchFilters, SearchQueryType, SearchRequest, SearchResponse, SearchResult };
//# sourceMappingURL=types.d.ts.map