import { AiSearchIndexSummary, AiSearchRequest, AiSearchResponse } from "./types.js";

//#region src/react/hooks/use-ai-search-query.d.ts
interface UseAiSearchQueryOptions {
  /**
   * Index alias to query. Defaults to the first index exposed by the plugin's
   * `clientConfig()`, so a single-index app needs no alias.
   */
  alias?: string;
}
interface UseAiSearchQueryResult<T extends Record<string, unknown> = Record<string, unknown>> {
  /** Run a search. Pass query text, or a full request for filters/paging control. */
  search: (query: string | AiSearchRequest) => Promise<AiSearchResponse<T> | null>;
  /** Latest response, null until the first successful search. */
  data: AiSearchResponse<T> | null;
  /** Whether a search is in progress. */
  loading: boolean;
  /** Error message, if any. */
  error: string | null;
  /** The resolved alias this hook queries. */
  alias: string | null;
  /** All configured indexes, for building a selector. */
  indexes: AiSearchIndexSummary[];
}
/**
 * Hook for querying a Databricks AI Search index. Reads the available indexes
 * from the ai-search plugin's `clientConfig()` and POSTs to
 * `/api/ai-search/{alias}/query`, so the UI never hardcodes an endpoint alias.
 */
declare function useAiSearchQuery<T extends Record<string, unknown> = Record<string, unknown>>(options?: UseAiSearchQueryOptions): UseAiSearchQueryResult<T>;
//#endregion
export { UseAiSearchQueryOptions, UseAiSearchQueryResult, useAiSearchQuery };
//# sourceMappingURL=use-ai-search-query.d.ts.map